spring:
  application:
    name: financial-recon
  
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:XE
    username: ${DB_USERNAME:your_username}
    password: ${DB_PASSWORD:your_password}
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
        jdbc:
          time_zone: UTC

  batch:
    job:
      enabled: false  # Set to false, manually trigger Job
    jdbc:
      initialize-schema: always  # Automatically initialize Spring Batch metadata tables
      table-prefix: BATCH_  # Spring Batch table prefix

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.xxcards.xbtx.udar: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

